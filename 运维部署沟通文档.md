# V0 Sandbox 项目部署需求

## 📋 项目基本信息

**项目名称**: V0 Sandbox - AI 代码沙箱平台  
**项目类型**: Next.js 15 全栈 Web 应用  
**技术栈**: React + TypeScript + Prisma + Docker  
**部署环境**: 生产环境  
**预期用户**: 开发团队内部使用  

## 🎯 项目功能简介

这是一个基于AI的代码生成和在线编辑平台，主要功能包括：
- AI 智能代码生成
- 在线代码编辑器
- 实时预览和调试
- 项目文件管理
- 模板系统

## ⚠️ 重要！服务器配置要求

### 🔐 **安全警告**
**此项目包含动态代码执行功能，存在安全风险！**
- 用户可以运行任意代码
- 需要访问 Docker Socket
- 动态创建和管理容器
- **建议部署在隔离环境或内网**

### 硬件要求（关键）
- **CPU**: 最低 4核，推荐 8核
- **内存**: **最低 8GB**（重要！低于此配置会构建失败）
- **磁盘**: 最低 50GB SSD
- **网络**: 需要外网访问（调用AI服务）

### 软件环境要求
- **操作系统**: Ubuntu 20.04+ 或 CentOS 7+
- **Node.js**: 18.0+
- **Docker**: 20.0+
- **Docker Compose**: V2 (docker compose)
- **Nginx**: 用于反向代理（可选）

### 🛡️ 安全配置要求
- **Docker Socket 访问**: 应用需要访问 `/var/run/docker.sock`
- **容器隔离**: 建议配置 AppArmor/SELinux
- **网络隔离**: 建议使用独立的 Docker 网络
- **资源限制**: 必须配置容器资源限制（内存、CPU）
- **用户权限**: Docker 用户需要适当权限

## 📦 项目部署包内容

我已经为您准备好了完整的部署包，包含：

1. **自动部署脚本** - `deploy.sh`
2. **系统环境检查** - `check-requirements.sh`  
3. **Docker 配置** - `docker-compose.yml`, `Dockerfile.prod`
4. **环境变量模板** - `env.template`
5. **详细部署文档** - `DEPLOYMENT-CHECKLIST.md`
6. **Nginx 配置** - `nginx.conf`

## 🚀 推荐部署流程

### 方案一：自动化部署（推荐）
```bash
# 1. 检查系统环境
./check-requirements.sh

# 2. 如果检查通过，一键部署
sudo ./deploy.sh production
```

### 方案二：Docker 部署
```bash
# 1. 配置环境变量
cp env.template .env.local
# 编辑 .env.local 填入具体值

# 2. 启动服务
docker-compose up -d
```

## 🔧 需要配置的环境变量

### 必需配置
```bash
NODE_ENV=production
NEXTAUTH_SECRET=请生成32位随机字符串
NEXTAUTH_URL=https://部署域名
DATABASE_URL="file:./data/prod.db"
```

### AI服务配置（至少配置一个）
```bash
# Dify AI（推荐）
DIFY_API_KEY=需要申请
DIFY_API_ENDPOINT=https://api.dify.ai/v1

# 或 OpenAI
OPENAI_API_KEY=需要申请
```

## 🔍 部署验证

部署完成后，请帮我验证：
- [ ] 主页访问：`http://服务器IP:3000`
- [ ] 健康检查：`http://服务器IP:3000/api/health` 返回 200
- [ ] 服务状态：`docker-compose ps` 显示 Up 状态

## 📊 监控配置

项目包含 Grafana + Prometheus 监控：
- **Grafana**: `http://服务器IP:3001` (admin/admin123)
- **Prometheus**: `http://服务器IP:9090`

## 🛡️ 重要安全配置

### Docker 安全设置
```bash
# 1. 配置 Docker 守护进程安全选项
sudo tee /etc/docker/daemon.json << 'EOF'
{
  "userns-remap": "default",
  "live-restore": true,
  "userland-proxy": false,
  "no-new-privileges": true
}
EOF

# 2. 重启 Docker 服务
sudo systemctl restart docker
```

### 防火墙和网络隔离
```bash
# 仅允许必要端口
ufw allow 22/tcp     # SSH
ufw allow 80/tcp     # HTTP  
ufw allow 443/tcp    # HTTPS
ufw allow 3000/tcp   # 主应用（仅内网）
ufw --force enable

# Docker 网络隔离
docker network create --driver bridge sandbox-isolated
```

### 容器资源限制（已配置）
- 内存限制: 每个沙箱容器 512MB
- CPU 限制: 每个沙箱容器 0.5 核心
- 磁盘配额: 每个项目 1GB

### 监控要求
- 监控异常容器创建
- 监控资源使用情况  
- 监控网络连接
- 设置告警阈值

## ⚡ 已知问题及解决方案

1. **构建内存不足**
   - 问题：构建时 OOM 错误
   - 解决：确保服务器至少 8GB 内存

2. **依赖包下载失败**  
   - 问题：`@next/swc` 包 401 错误
   - 解决：清理缓存重新安装

3. **端口冲突**
   - 检查端口 3000、3001、9090 是否被占用

4. **Docker Socket 权限问题**
   - 问题：无法访问 Docker Socket
   - 解决：确保应用用户在 docker 组中
   
5. **容器资源耗尽**
   - 问题：创建过多容器导致资源不足
   - 解决：配置自动清理机制（已实现）

## 🎯 我的支持

- **部署文档**：已提供完整的 `DEPLOYMENT-CHECKLIST.md`
- **技术支持**：部署过程中如有问题，随时联系我
- **应急方案**：如自动脚本失败，可使用手动部署步骤

## 📅 时间安排

- **预计部署时间**：1-2小时（包含环境准备）
- **测试验证时间**：30分钟
- **总体时间**：2-3小时

## 🔗 访问信息

部署完成后，请提供：
- [ ] 服务器IP地址
- [ ] 访问域名（如有）
- [ ] 管理员账号信息

---

**注意事项**：
1. 生产环境部署前，建议在测试环境先验证
2. 请确保服务器满足硬件要求，特别是内存
3. 如需要SSL证书，请一并考虑

感谢配合！有任何问题请及时反馈。
